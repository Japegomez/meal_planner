// ignore_for_file: non_constant_identifier_names, camel_case_types, file_namesimport, file_names, unnecessary_null_comparison, prefer_null_aware_operators
// WARNING: This code is auto-generated by Supadart.
// WARNING: Modifications may be overwritten. Please make changes in the Supadart configuration.
import 'supadart_header.dart';

class WeeklyPlan implements SupadartClass<WeeklyPlan> {
  final String id;
  final String? householdId;
  final String? userId;
  final DateTime weekStart;
  final DateTime createdAt;

  const WeeklyPlan({
    required this.id,
    this.householdId,
    this.userId,
    required this.weekStart,
    required this.createdAt,
  });

  static String get table_name => 'weekly_plans';
  static String get c_id => 'id';
  static String get c_householdId => 'household_id';
  static String get c_userId => 'user_id';
  static String get c_weekStart => 'week_start';
  static String get c_createdAt => 'created_at';

  static List<WeeklyPlan> converter(List<Map<String, dynamic>> data) {
    return data.map(WeeklyPlan.fromJson).toList();
  }

  static WeeklyPlan converterSingle(Map<String, dynamic> data) {
    return WeeklyPlan.fromJson(data);
  }

  static Map<String, dynamic> _generateMap({
    String? id,
    String? householdId,
    String? userId,
    DateTime? weekStart,
    DateTime? createdAt,
  }) {
    return {
      if (id != null) 'id': id,
      if (householdId != null) 'household_id': householdId,
      if (userId != null) 'user_id': userId,
      if (weekStart != null) 'week_start': weekStart.toIso8601String(),
      if (createdAt != null) 'created_at': createdAt.toUtc().toIso8601String(),
    };
  }

  static Map<String, dynamic> insert({
    String? id,
    String? householdId,
    String? userId,
    required DateTime weekStart,
    DateTime? createdAt,
  }) {
    return _generateMap(
      id: id,
      householdId: householdId,
      userId: userId,
      weekStart: weekStart,
      createdAt: createdAt,
    );
  }

  static Map<String, dynamic> update({
    String? id,
    String? householdId,
    String? userId,
    DateTime? weekStart,
    DateTime? createdAt,
  }) {
    return _generateMap(
      id: id,
      householdId: householdId,
      userId: userId,
      weekStart: weekStart,
      createdAt: createdAt,
    );
  }

  factory WeeklyPlan.fromJson(Map<String, dynamic> jsonn) {
    return WeeklyPlan(
      id: jsonn['id'] != null ? jsonn['id'].toString() : '',
      householdId: jsonn['household_id'] != null
          ? jsonn['household_id'].toString()
          : null,
      userId: jsonn['user_id'] != null ? jsonn['user_id'].toString() : null,
      weekStart: jsonn['week_start'] != null
          ? DateTime.parse(jsonn['week_start'].toString())
          : DateTime.fromMillisecondsSinceEpoch(0),
      createdAt: jsonn['created_at'] != null
          ? DateTime.parse(jsonn['created_at'].toString())
          : DateTime.fromMillisecondsSinceEpoch(0),
    );
  }

  static Object New({
    String? id,
    String? householdId,
    String? userId,
    DateTime? weekStart,
    DateTime? createdAt,
  }) {
    return {
      if (id != null) 'id': id,
      if (householdId != null) 'household_id': householdId,
      if (userId != null) 'user_id': userId,
      if (weekStart != null) 'week_start': weekStart,
      if (createdAt != null) 'created_at': createdAt,
    };
  }

  Map<String, dynamic> toJson() {
    return _generateMap(
      id: id,
      householdId: householdId,
      userId: userId,
      weekStart: weekStart,
      createdAt: createdAt,
    );
  }

  static const _unset = Object();
  WeeklyPlan copyWith({
    Object? id = _unset,
    Object? householdId = _unset,
    Object? userId = _unset,
    Object? weekStart = _unset,
    Object? createdAt = _unset,
  }) {
    return WeeklyPlan(
      id: id == _unset ? this.id : id as String,
      householdId: householdId == _unset
          ? this.householdId
          : householdId as String?,
      userId: userId == _unset ? this.userId : userId as String?,
      weekStart: weekStart == _unset ? this.weekStart : weekStart as DateTime,
      createdAt: createdAt == _unset ? this.createdAt : createdAt as DateTime,
    );
  }
}
